<%= form_for(@motion) do |f| %>
  <% if @motion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@motion.errors.count, "error") %> prohibited this motion from being saved:</h2>

      <ul>
      <% @motion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, "Antragstitel:" %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :text, "Antragstext:" %><br />
    <%= f.text_area :text %>
  </div>
  <div class="field">
    <%= f.label :date, "Antragsdatum:" %><br />
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :register, "Aktenzeichen:" %><br />
    <%= f.text_field :register %>
  </div>
  <div class="field">
    <%= f.label :latitude %><br />
    <%= f.text_field :latitude %>
  </div>
  <div class="field">
    <%= f.label :longitude %><br />
    <%= f.text_field :longitude %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
